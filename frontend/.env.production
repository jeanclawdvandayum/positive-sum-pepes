# production overrides for the published build (vite build mode=production)
# — no dev proxy on the host; use the CORS-open public RPC (no key in bundle)
VITE_RPC_URL=https://base-sepolia-rpc.publicnode.com
