# Alternative UI release, September 13

Local URL: http://127.0.0.1:4180/

Factory: `0x6126f0a736c4d137bf3b99d0f79807b6faf19f8f` on Base Sepolia.

Both the IBCO window and maximum countdown are 600 seconds. The other final-spec
settings remain: uncapped IBCO and wallets, 69 seconds per ticket, linear 0.42%
ticket growth, IBCO-scaled sine curve, four-week withdrawal horizon and lePSP.

The live alternative frontend is in `frontend/src/alt`. The original frontend
and static design prototype remain. `npm run dev:alt` uses the ignored local
profile. RPC credentials stay in the server-side proxy setting.

The Ethereum registrar keeps existing names. Its eligibility gate and local
verifier now target the new factory. The verifier accepts the local 4180 origin.

Validation: 553 contract tests, 177 frontend tests, 11 verifier tests, deterministic
size/ABI/art/math checks, 17 successful deployment receipts, and two deployed-code
fork lifecycle tests. Browser tests on Anvil covered reserved-art predeposit,
buying, genesis claiming, real ladder data, individual NFT controls and graveyard
winner labels. Both trade panels measured 911 pixels high. Mobile width stayed
390 pixels at a 390-pixel viewport. Live predeposit reads succeeded after wiring.

The release snapshot is `078cf41bf1a9a9eb2780fd004e4250c3c283134d` in a separate local checkout. The
manifest checks deployed runtime bytes against its compiled artifacts. Public
source explorer verification was not run in this release.

See [manifest](deployments/base-sepolia-2026-09-13-alt.json) and
[name migration](deployments/base-sepolia-2026-09-13-alt-names.json).
