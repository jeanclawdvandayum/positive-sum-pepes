# September 10 Base Sepolia release

Source revision: `57f112a50e9c8ce6131329c2ab10f029d5a2aec7`.
Factory: `0xe4bf7a7450b580fef3a5965e4f3896e736e06dd2`.
See the [confirmed manifest](deployments/base-sepolia-2026-09-10.json).

- Predeposit: 86,400 seconds (24 hours), global cap 1,000 mixETH, wallets uncapped.
- Maximum active clock: 15,600 seconds (04:20:00). Each full 0.005 mixETH buy unit still adds up to 260 seconds.
- Withdrawal vest: unchanged at one hour.
- Art version 2: 191,664,000 combinations. Predeposit reserves the user's chosen art. Reserved and minted canonical combinations remain unavailable to other mints.

All 17 Base Sepolia deployment receipts succeeded. The manifest compares deployed runtime and exact immutable art storage with the build, checks wiring and reads the profile from chain. Both deployed lifecycle tests passed, covering a fresh round, successor round and old-round exits.

The deterministic gate passed 537 Solidity tests in 70 suites, 160 frontend tests, 11 verifier tests, 161 ABI declarations, 38 production size checks, the independent sine oracle and governance gate. TypeScript and the production UI build passed.

[Sourcify record](deployments/base-sepolia-2026-09-10-verification.json): 16/17 creation and runtime matches. The external Linux solc 0.8.26 compiler crashes on `PepeExpandedDescriptor`, including a retry with only its import dependencies. This is an outstanding public source-verification limitation. Local runtime/art-byte comparisons and expanded-renderer tests passed. Explorer forwarding also hit provider submission quotas.

The predeposit page uses a compact two-column desktop grid, with the selector beside progress and deposit controls. At 1366×768 the controls fit within the first viewport. Mobile stays single-column with no horizontal overflow. The UI and its bundled rolling paper use the new 04:20:00 maximum. The footer links to `/whitepaper.html` on the same host.

The existing Ethereum registrar and names are retained. Its new eligibility gate is `0x6f6813a47d8fc014b331bcb6e93e2bba12a29c3a`, targeting the new factory. Registration is enabled. The local verifier is rewired and restarted. [Name migration receipts](deployments/base-sepolia-2026-09-10-names.json).

Free-name verification still uses the existing loopback verifier URL. Hosting the static UI does not expose that service to remote testers.

UI: https://nimble-lodge-tnt6.here.now/ . Bundled paper: https://nimble-lodge-tnt6.here.now/whitepaper.html . Anonymous publication expires September 11 at 04:23 UTC unless claimed. Local verifier CORS includes this origin.
