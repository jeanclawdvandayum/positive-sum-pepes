# Sine rules v3 — implementation and numerical validation note

**Date:** 2026-09-14/15 · **Branch:** `single-sine-spec` (base `deployment-spec`)
**Change:** one continuous tilted-sine curve (IBCO + active), softened cube-root
growth to 1,000× launch price at wave ten, square-root wavelength scaling,
pot-priced ladder tickets with the active minimum equal to exactly one spot,
ticket-intent guards on the interface purchase routes. Source and local
changes only — no public deployment, publication, or live-address change.

## What changed on-chain

- **`src/SineV3Math.sol` (new, 12,128 B runtime)** — the shared, read-only
  settlement helper. Deployed once per factory by the deploy script, pinned
  immutable in the factory constructor, and passed to every round's hook at
  birth (`configureSineV3(pL, table)`). No storage, no owner, no
  configuration: the canonical table is the code. Functions: `lamAt`,
  `targetAt`, `priceWad`, `supplyWad`, `genesisQ`, `buyOut`, `sellOut`.
- **`src/SineV3Knots.sol` (generated)** — 321 canonical quarter-wave knots of
  `F(x) = ∫₀ˣ exp(−K·H(s(t)))dt` on x ∈ [−16, +64] waves, generated by
  `scripts/sine_v3.py` (70-digit Decimal, cumulative 16-point Gauss-Legendre,
  split-cell certified below 1e-27 relative; WAD-floored; balanced dispatch
  keeps reads O(log n) with no SLOADs). `K_WAD = 5643682713637197225`.
- **`CurveHook`** — v2 sine paths replaced by helper calls; `SINE_RULES_VERSION`
  and `TICKET_RULES_VERSION` → 3; `ticketPrice() = ceil(pot/10⁴)` min 1 wei;
  `MIN_BUY_INPUT()` becomes a dynamic getter equal to `ticketPrice()` on v3
  rounds (legacy rounds keep 0.005); the v3 buy minimum is exactly one spot
  sampled pre-own-fee; 64-byte hookData `{trader, maxTicketPrice}` enables
  opt-in ticket-intent guards (`TicketPriceMoved`); version-3 getter
  `sineV3Info()`. Runtime **shrank** 23,918 → 20,228 B.
- **`PSPFactory`** — `sineV3Table` immutable; `configureSineV3(pL)`; spawn
  context hashes include pL + helper address.
- **`RoundController`** — `_validateBootstrap` rejects net boots above
  `BOOT_NET_MAX = (16·955e18)²/450e18 ≈ 518,841 mixETH` and dry-runs the exact
  genesis on every deposit; the v2 ≥450-mixETH-only validation is gone
  (v3 has no dust cliff — one wei boots materialize).
- **`PSPZapIn` / `PSPReferralRegistry`** — guarded routes
  `buyWithMixGuarded` / `buyWithMixForGuarded` / `buyWithMixGuarded`
  (registry); legacy selectors unchanged.
- **`PSPReinvestor`** — minimum read live from the round's hook.
- v2 `SineMath.sol` and its library tests remain untouched for legacy
  reference; no `src/` consumer links it.

## Numerical architecture

Supply is `Q(R) = (λ/P_L)·(F(x(R)) − F(x(0)))`. `F` is evaluated from the
knot table plus ONE 8-point Gauss-Legendre partial cell at each interval end
— O(1) work per evaluation regardless of waves crossed (measured: buys
spanning ~60 waves cost the same order as quarter-wave buys). Partial cells
are clamped inside their knot bracket (the AUD-6 pattern), so the rounded `F`
never steps down: supply is monotone by construction. Sells invert `Q` with
a bracketed Newton/bisection hybrid on [0, R] (Q strictly increasing
guarantees termination; proportional seed; 128-iteration bound; upper
endpoint returned, never overpaying), plus the independent sell spot clamp.

**Accuracy (independent Decimal reference):**
- Prices match the specification to ≤ 1e-11 relative on all fixture rows
  (measured ≤ ~4e-15 at reference scales; the binding constraint deep in the
  16-wave prelaunch tail is expWad's ~1e-12, still beyond the goal).
- Supplies match to ≤ 1e-9 relative (measured ≤ 4e-15).
- All eleven wave-milestone prices match the closed-form schedule
  1000^(((1+n)^{1/3}−1)/(11^{1/3}−1)) to 1e-9 (spec table verified).
- `P(b+d)·P(b−d) = P_L²` to 22 digits; λ(450e18) = 955e18 exactly;
  λ(1 wei) = 45,019,131,735; λ(4b) = 2λ(b) within rounding.
- Worst GL8 partial-cell error vs the reference: 3.8e-17 relative.
- expWad (solady) measured across the v3 argument range: ≤ 2e-17 for
  |x| ≤ 10 wad; the domain cap (16 prelaunch waves) keeps every operating
  point inside ~1e-12.

**Oracle independence:** knots are built by cumulative GL16; fixture supplies
are computed by tanh-sinh (double-exponential) quadrature — a different
method — with level-doubling convergence; milestone prices are closed-form.
Nothing is derived from Solidity output. `scripts/sine_v3.py --check`
regenerates both files byte-for-byte (~66 s, in the deterministic gate).

## Notable fixes found by the machine proofs during migration

- **Inverted inverse seed (Critical-class, caught by the custody fuzz):**
  the sell-inverter's proportional seed was subtracted from R instead of
  used as the candidate, making sells of ~0.2% of supply revert ZeroOutput
  (worst-case bracket never closed). Fixed; the exact failing case now
  returns the manual-bisection answer to the wei.
- H-identity scale bug (`c·c` raw vs WAD) — prices collapsed to P_L
  everywhere; caught by the first oracle run.
- Prelaunch supply sign handling (`uint256(fR)` on negative F) — overflow
  panic on R < boot; caught by the probe suite.

## Tests and gates (see GATE-LOG for the full counts)

- New suites: `SineV3Oracle` (6 — independent fixtures, milestones,
  adjacent-wei monotonicity, domain edges), `TicketRulesV3` (7 — the §8.2
  integer table through real V4, launch price table 0.0005→0.02 across six
  rebirths, exact minimum below 1e12 wei, pre-own-fee sampling, sell-fee
  repricing, donation immunity, guarded route), `SineV3Economics` (6 —
  full-cycle conservation fuzz through real V4, helper-level curve-leg
  conservation, integer telescoping, flat multi-wave gas, bounded inverse
  gas, domain-failure rollback).
- Migrated: `SineCustodyInvariants` (P1/P2 now gate the v3 helper), stateful
  handlers derive amounts from the live ticket price, capacity suites
  rescoped to the v3 supported band with the protections preserved
  (int128 input rejection, wave-domain capacity error, supply-guard sells).
- Sizes: CurveHook 20,228 B · SineV3Math 12,128 B · factory 19,9xx B —
  every production artifact under EIP-170 with ≥ 4.3 KB hook headroom.
- Slither: 0 High / 0 Medium (informational baseline grew 242 → 267; new
  entries are uninitialized-local/should-emit-event class, reviewed).

## Frontend

`sineVersion` accepts 3 (v1/v2 formulas preserved verbatim); `sineChart`
renders the v3 formula with eleven wave markers on waves −1..+12; `gameRules`
/ `useRound` expose the live `ticketPrice` as the minimum (BigInt
end-to-end); `SwapCard` routes ordinary buys through the guarded variants
passing the quoted price; `PepeCards`/`Stake` reinvest eligibility uses the
live minimum; predeposit stays any-positive (0.005 fallback only below
version 3); alt UI consumes the same live rules; rolling-paper factual
claims updated (formula 5, ticker, 03A caption). Legacy chart decode keeps
working through explicitly-marked legacy-only ABI entries.

## Known limitations

- The knot table and domain edges (−16 / +64 waves) are fixed constants of
  the helper: net backing above ~518,841 mixETH or price above ~1,927
  mixETH/PSP cannot be represented. Both revert loudly before state changes.
- expWad precision in the deep prelaunch tail (> ~8 waves below launch,
  i.e. raises above ~500k mixETH) degrades toward 1e-12 — inside the price
  goal but documented.
- Deployment manifests record v3 after the next authorized deployment; the
  historical manifests in `docs/audit/deployments/` are untouched.
