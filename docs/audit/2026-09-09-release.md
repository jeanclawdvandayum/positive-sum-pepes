# September 9 testnet release

Factory: `0x182307cfd9d1fac0b2e69cb862e993314332d2cb` on Base Sepolia (84532).
Source revision: `302e233c`.

- Predeposit: 86,400 seconds from final round wiring, 1,000 mixETH total cap,
  any positive amount and no per-wallet cap.
- Active clock: 248,660 seconds (69:04:20), including its refill cap.
- Withdrawal vest remains 3,600 seconds across six epochs.
- Art reservation v2 selects and reserves the Pepe in the funded deposit.
  Top-ups keep it; both claim paths mint it, including after detonation.
- UI env files use factory-derived addresses, new history start, routers,
  mock mixETH/faucet and reinvestor. The footer links to the rolling paper.
- Existing Ethereum registrar retains domain custody and names. Eligibility
  gate `0x8e65b2eedaebc97abc7444ffcdae6dd7f8278ce2` targets the fresh factory.
  All three name administration receipts succeeded. The local verifier was
  configured and restarted. Remote free-name verification still requires an
  externally reachable verifier endpoint; the current endpoint is loopback.

Validation: 531 Solidity tests / 69 suites, 160 frontend tests, 11 verifier
checks, ABI/size/oracle/governance gates and production build. Simulation
preceded broadcasting. All 17 PSP deployment receipts succeeded, 17/17
creation/runtime sources matched on Sourcify, and both deployed lifecycle
fork tests passed (including a chosen deposit and successor).

Live reads independently pinned 24h / 1,000 mixETH / 69:04:20 and art version 2.
The 390px UI smoke confirmed the picker, cap, footer link and no overflow.
Rolling paper checks covered 390/1280px and the new simulator clock/boot values.

See the `deployments/base-sepolia-2026-09-09*.json` records. Earlier contracts
and their positions remain at their original addresses.

Hosted UI: https://stormy-quill-wawf.here.now/

Rolling paper: https://deep-pepper-sy7v.here.now/

Both anonymous publications expire after 24 hours unless claimed.
